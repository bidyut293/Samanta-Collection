// Placeholder until real types are generated from the live schema:
//   npx supabase gen types typescript --project-id giojyegdwuywtouyrrtg > lib/supabase/types.ts
// Run that once supabase/migrations/*.sql has been pushed to the project,
// then replace this file's contents with the generated output.
export type Database = Record<string, unknown>;
